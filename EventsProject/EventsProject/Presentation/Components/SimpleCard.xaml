<UserControl x:Class="EventsProject.Presentation.Components.SimpleCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EventsProject.Presentation.Components"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             
             mc:Ignorable="d">
    <Grid>
        <Border Style="{StaticResource brdCard}">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <fa:FontAwesome Icon="{Binding Icon, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    Style="{StaticResource fnaBrdCard}"/>
                    
                    <Label Content="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           Style="{StaticResource lblBrdCard}"/>
                </StackPanel>
                <Label Content="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Style="{StaticResource lblBrdCard}"/>
                
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
