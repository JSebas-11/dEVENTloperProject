<dialogs:CustomDialog x:Class="EventsProject.Presentation.UI.Forms.RestorePasswordForm"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EventsProject.Presentation.UI.Forms"
      xmlns:dialogs="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
      xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
      xmlns:fa="http://schemas.fontawesome.io/icons/"
      
      mc:Ignorable="d"
      Height="{StaticResource formHeight}" Width="{StaticResource formWidth}"
      DialogContentWidth="Auto"
      Background="{StaticResource linerGradBG}">

    <!-- MAIN LAYOUT -->
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <!-- MAIN LAYOUT - HEADER -->
        <StackPanel Orientation="Horizontal" Margin="0 10">
            <StackPanel>
                <fa:FontAwesome Icon="InfoCircle" Foreground="{StaticResource clrTxt}"/>
                <TextBlock Text="Your code will be valid even if you close this dialog"
                               Style="{StaticResource fntSbTitleTxb}" Foreground="{StaticResource clrTxt}" 
                               TextAlignment="Center" TextWrapping="Wrap" Width="380"/>

            </StackPanel>

            <Button x:Name="btnBack" Click="btnBack_Click" Margin="75 25 0 25">
                <fa:FontAwesome Icon="Close" Style="{StaticResource fnaAnimatedBtn}"/>
            </Button>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0 20">
            <Label Content="Welcome to the changer password menu, " Style="{StaticResource fntText}" VerticalAlignment="Center"/>
            <Label x:Name="lblUserName" Style="{StaticResource fntSbTitle}"/>
        </StackPanel>

        <!-- MAIN LAYOUT - PASSWORD -->
        <StackPanel Orientation="Horizontal" Margin="0 0 0 5">
            <Label Content="New Password:" Style="{StaticResource fntSbTitle}" Foreground="{StaticResource clrTxtSec}"/>
            <PasswordBox x:Name="pwbPassword" PasswordChar="*" Style="{StaticResource styTextBox}" 
                             MaxLength="20" Width="250" Margin="36 0 0 0"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0 5 0 0">
            <Label Content="Confirm Password:" Style="{StaticResource fntSbTitle}" Foreground="{StaticResource clrTxtSec}"/>
            <PasswordBox x:Name="pwbPasswordConfirm" PasswordChar="*" Style="{StaticResource styTextBox}" MaxLength="20" Width="250"/>
        </StackPanel>

        <Button x:Name="btnConfirmPassword" Click="btnConfirmPassword_Click"  HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 50">
            <StackPanel Orientation="Horizontal">
                <fa:FontAwesome Icon="CheckCircle" Style="{StaticResource fnaAnimatedBtn}"/>
                <TextBlock Text="Confirm New Password" Margin="8 0 0 0"/>
            </StackPanel>
        </Button>

    </StackPanel>

</dialogs:CustomDialog>
