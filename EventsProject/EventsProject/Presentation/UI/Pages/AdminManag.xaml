<Page x:Class="EventsProject.Presentation.UI.Pages.AdminManag"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EventsProject.Presentation.UI.Pages"
      xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
      xmlns:fa="http://schemas.fontawesome.io/icons/"
      
      mc:Ignorable="d" 
      Title="AdminManag">

    <!-- MAIN LAYOUT -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height=".5*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height=".2*"/>
            <RowDefinition Height="1.8*"/>
            <RowDefinition Height=".05*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width=".25*"/>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="1.5*"/>
            <ColumnDefinition Width=".25*"/>
        </Grid.ColumnDefinitions>

        <!-- MAIN LAYOUT - HEADER -->
        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
            <fa:FontAwesome Icon="Gears" Margin="0 0 10 0" Style="{StaticResource fnaAnimatedFade}"/>
            <Label Content="Events Managment" Style="{StaticResource fntTitle}" 
                   Foreground="{StaticResource clrTxt}" VerticalAlignment="Center"/>
        </StackPanel>
        <Button Grid.Row="0" Grid.Column="1" x:Name="btnRefresh" Click="btnRefresh_Click" Margin="800 5 0 5">
            <StackPanel Orientation="Horizontal">
                <fa:FontAwesome Icon="RotateRight" Style="{StaticResource fnaAnimatedBtn}"/>
                <TextBlock Text="Refresh" Margin="8 0 0 0"/>
            </StackPanel>
        </Button>
        <Button Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="2" x:Name="btnCreateEvent" Click="btnCreateEvent_Click" Margin="5">
            <StackPanel Orientation="Horizontal">
                <fa:FontAwesome Icon="Plus" Style="{StaticResource fnaAnimatedBtn}"/>
                <TextBlock Text="New Event" Margin="8 0 0 0"/>
            </StackPanel>
        </Button>

        <!-- MAIN LAYOUT - FILTER-SEARCH SECTION -->
        <StackPanel Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" HorizontalAlignment="Center">
            
            <Label Content="Search and Filter" Style="{StaticResource fntTitle}" Foreground="{StaticResource clrTxt}" HorizontalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <fa:FontAwesome Icon="Filter" Style="{StaticResource fnaAnimatedFade}" Foreground="{StaticResource clrHover}"/>
                <Label Content="Filter By: " Style="{StaticResource fntSbTitle}" Foreground="{StaticResource clrTxt}"/>
                <ComboBox x:Name="cmbFilters" FontSize="22" Margin="0 2" SelectionChanged="cmbFilters_SelectionChanged"/>
            </StackPanel>
            <mah:DateTimePicker x:Name="dtpDateFilter" Cursor="Hand" Culture="en-MT" Style="{StaticResource styTextBox}" Visibility="Collapsed"/>
            <ComboBox x:Name="cmbFilterInput" FontSize="22" Margin="0 2" Visibility="Collapsed"/>
            <TextBox x:Name="txbFilterInput" MaxWidth="425" Style="{StaticResource styTextBox}" MaxLength="32" Visibility="Collapsed"/>
            
            <Button Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="2" x:Name="btnFilter" Click="btnFilter_Click" 
                    Margin="5" Width="235">
                <StackPanel Orientation="Horizontal">
                    <fa:FontAwesome Icon="Search" Style="{StaticResource fnaAnimatedBtn}"/>
                    <TextBlock Text="Apply and Search" Margin="8 0 0 0"/>
                </StackPanel>
            </Button>
            
        </StackPanel>

        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
            <fa:FontAwesome Icon="Database" FontSize="22" VerticalAlignment="Center" Foreground="{StaticResource clrTxtSec}"/>
            <Label Content="Results: " Style="{StaticResource fntSbTitle}" Foreground="{StaticResource clrTxtSec}"/>
            <Label x:Name="lblNumResults" Style="{StaticResource fntSbTitle}" Foreground="{StaticResource clrTxt}"/>
        </StackPanel>
        
        <!-- MAIN LAYOUT - DATA SECTION -->
        <DataGrid x:Name="dgEvents" Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" ToolTip="Double click in a field to edit"
                  RowEditEnding="dgEvents_RowEditEnding" Style="{StaticResource styleDgCustomized}" 
                  RowStyle="{StaticResource styleDgRCustomized}" ColumnHeaderStyle="{StaticResource styleDghCustomized}">
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="🔒 Event ID" IsReadOnly="True" Binding="{Binding EventId}"/>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}"/>
                <DataGridTextColumn Header="Description" Binding="{Binding EventDescription}" Width="100"/>
                <DataGridTextColumn Header="Capacity" Binding="{Binding Capacity}"/>
                <DataGridComboBoxColumn x:Name="dgEventsCmbCategory" Header="Category"
                                        SelectedValueBinding="{Binding CatId}" SelectedValuePath="CatId"
                                        DisplayMemberPath="CatName"/>
                <DataGridComboBoxColumn x:Name="dgEventsCmbState" Header="State"
                                        SelectedValueBinding="{Binding EventStateId}" SelectedValuePath="EventStateId"
                                        DisplayMemberPath="StateName"/>
                <DataGridTextColumn Header="Place" Binding="{Binding EventPlace}"/>
                <DataGridTextColumn Header="🔒 Initial Time" IsReadOnly="True" Binding="{Binding InitialTime}"/>
                <DataGridTextColumn Header="🔒 End Time" IsReadOnly="True" Binding="{Binding EndTime}"/>
                <DataGridTextColumn Header="City" Binding="{Binding EventCity}"/>
                <DataGridTextColumn Header="🔒 Duration (mins)" IsReadOnly="True" Binding="{Binding MinutesDuration}"/>
                <DataGridTextColumn Header="Artists" Binding="{Binding Artist}" Width="100"/>
            </DataGrid.Columns>
            
        </DataGrid>

    </Grid>
</Page>
